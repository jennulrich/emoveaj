{% extends 'admin/layout-admin.html.twig' %}

{% block content %}
    <style>
        .add-title div label {
            width: 10%;
        }

        .add-title div input {
            width: 50%;
        }
    </style>
    <div class="row">
        <div class="col-md-6" style="margin-top: 20px; margin-bottom: 15px">
            <h2>Modifier un modèle Voiture</h2>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6"></div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <div class="card" style="padding: 15px">
                <div class="form container-fluid">
                    {{ form_start(form) }}
                    {{ form_errors(form) }}
                    <div class="add-title">
                        {{ form_row(form.brand) }}
                    </div>
                    <div class="add-title">
                        {{ form_row(form.name_model) }}
                    </div>
                    <div class="add-title">
                        {{ form_row(form.autonomie) }}
                    </div>
                    <div class="add-title">
                        {{ form_row(form.gamme) }}
                    </div>
                    <div class="row">
                        <div class="col-md-6">
                            <button type="submit" class="btn add text-uppercase btnEdit" id="add-button">Modifier</button>
                            <a href="{{ path('admin_car_models') }}" class="btn back-add text-uppercase">annuler</a>
                        </div>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

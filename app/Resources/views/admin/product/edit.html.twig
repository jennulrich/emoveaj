{% extends 'admin/layout-admin.html.twig' %}

{% block content %}
    <a href="{{ path('product_index') }}"><button type="button" class="btn return"><i class="fa fa-arrow-left"></i>Retour à la liste</button></a>

    <div class="card">
        <div class="card-body">
            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <input type="submit" value="Modifier" class="delete" style="margin-top: 25px;"/>
            {{ form_end(edit_form) }}
        </div>
    </div>
{% endblock %}
